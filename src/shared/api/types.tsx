export type MessageResType = {
  message: string;
};

export type UserType = {
  id: string;
  avatar: string;
  name: string;
  email: string;
  about: string;
  isBlocked: boolean;
  isAdmin: boolean;
};
